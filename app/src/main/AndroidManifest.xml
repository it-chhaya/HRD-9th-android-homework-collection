<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.homework.collection">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.9thHomeworkCollection">
        <activity
            android:name=".demo.ResultActivity"
            android:exported="true" />
        <activity
            android:name=".demo.RegisterActivity"
            android:exported="true" >

        </activity>
        <activity
            android:name=".design.LogInActivity"
            android:exported="true" />
        <activity
            android:name=".intent.UserActivity"
            android:exported="true" />
        <activity
            android:name=".intent.InformationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="image/*" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>